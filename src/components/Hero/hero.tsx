import React from 'react';
import "./hero.scss";

const Hero = () => {
    return (
        <section id="hero">
            <div className="container">
                <div className="hero">

                </div>
            </div>
        </section>
    );
};

export default Hero;